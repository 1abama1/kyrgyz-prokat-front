# 🔨 Инструкция по сборке проекта

## Сборка приложения

### Команда для сборки
```bash
npm run electron:build
```

Эта команда выполняет:
1. Компиляцию TypeScript (`tsc -b`)
2. Сборку React приложения (`vite build`)
3. Компиляцию Electron main процесса (`tsc -p tsconfig.node.json`)
4. Создание установочного файла (`electron-builder`)

### Результат сборки
После успешной сборки установочный файл будет находиться в:
```
release/Rental Desktop App Setup <version>.exe
```

## 📝 Логирование

### Где находятся логи (после установки приложения)

**Windows:**
```
C:\Users\<ИМЯ_ПОЛЬЗОВАТЕЛЯ>\AppData\Roaming\Rental Desktop App\logs\main.log
```

### Как найти путь к логам
При запуске приложения в консоли выводится точный путь к файлу логов:
```
📝 Log file location: C:\Users\...\logs\main.log
```

### Настройки логирования
- **Уровень**: info (записываются info, warn, error)
- **Максимальный размер файла**: 5MB
- **Формат**: `[2026-01-27 18:57:15.123] [info] текст сообщения`
- **Ротация**: автоматическая при достижении 5MB

### Что логируется
- ✅ Запуск приложения
- ✅ Версия приложения и Electron
- ✅ Платформа и системная информация
- ✅ Все операции с договорами (сохранение, открытие)
- ✅ Все ошибки и предупреждения

## 🚀 Разработка

### Запуск в режиме разработки
```bash
npm run electron:dev
```

В режиме разработки:
- Логи выводятся в консоль DevTools
- Автоматическая перезагрузка при изменении кода
- DevTools открываются автоматически

## 🐛 Отладка

### Просмотр логов в реальном времени
Вы можете открыть файл логов в любом текстовом редакторе и следить за изменениями в реальном времени.

### Увеличение уровня детализации
Измените в `electron/main.ts`:
```typescript
log.transports.file.level = "debug";  // вместо "info"
log.transports.console.level = "debug";
```

Это добавит более подробные логи для отладки.

## 📦 Структура проекта после сборки

```
release/
├── Rental Desktop App Setup <version>.exe  # Установочный файл
├── win-unpacked/                           # Распакованная версия (для тестирования)
└── builder-debug.yml                       # Отладочная информация сборки
```

## ⚠️ Важные замечания

1. **GPU Acceleration отключен** - это сделано для устранения ошибок рендеринга
2. **Меню приложения отключено** - для более чистого интерфейса
3. **Договоры сохраняются** в `C:\Users\<USER>\Documents\MishaCRM\Contracts\`
4. **Логи автоматически ротируются** при достижении 5MB
