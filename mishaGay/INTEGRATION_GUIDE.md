# 🔥 Инструкция по интеграции новой архитектуры Frontend

## ✅ Что было сделано

### 1. **Новые типы данных**
- ✅ `ToolCategory` (категория) - `/types/category.types.ts`
- ✅ `ToolTemplate` (модель инструмента) - `/types/template.types.ts`
- ✅ `Tool` (экземпляр) - обновлён в `/types/tool.types.ts`
- ✅ `CreateToolDto` - новый DTO для создания инструментов

### 2. **Новые API-клиенты**
- ✅ `categoriesAPI` - `/api/categories.ts`
  - `getAll()` - получить все категории
- ✅ `templatesAPI` - `/api/templates.ts`
  - `getAll()` - получить все модели
  - `getByCategory(categoryId)` - получить модели по категории
- ✅ `toolsAPI` - обновлён в `/api/tools.ts`
  - `getList()` - получить все экземпляры
  - `getByTemplate(templateId)` - получить экземпляры по модели
  - `getById(id)` - получить экземпляр по ID
  - `create(dto)` - создать новый экземпляр

### 3. **Обновлённые страницы**
- ✅ **ToolsPage** - теперь показывает дерево: Категория → Модель → Экземпляр
- ✅ **ToolCardPage** - использует `templateName` и `categoryName`
- ✅ **CreateToolPage** - два селекта: категория → модель
- ✅ **DashboardPage** - работает с новой структурой
- ✅ **CreateDocumentPage** - обновлён для работы с шаблонами
- ✅ **ClientCard** - обновлён для работы с новой структурой
- ✅ **CreateExcelContractInline** - обновлён
- ✅ **CreateRentalInline** - обновлён

---

## 📘 ШАГ 1: Проверка backend endpoints

Убедитесь, что ваш backend предоставляет следующие endpoints:

### Категории
```
GET /api/categories
```

### Модели (шаблоны)
```
GET /api/templates
GET /api/templates/category/{categoryId}
```

### Экземпляры инструментов
```
GET /api/tools
GET /api/tools/template/{templateId}
GET /api/tools/{id}
POST /api/tools
```

**Важно:** Если ваши endpoints находятся под `/api/admin/`, обновите пути в файлах:
- `mishaGay/src/api/categories.ts`
- `mishaGay/src/api/templates.ts`
- `mishaGay/src/api/tools.ts`

---

## 📘 ШАГ 2: Очистка кэша Vite

После обновления типов и API нужно очистить кэш:

```bash
cd mishaGay
rm -rf node_modules/.vite
# или на Windows:
rmdir /s /q node_modules\.vite
```

---

## 📘 ШАГ 3: Запуск проекта

```bash
cd mishaGay
npm install  # если нужно установить зависимости
npm run dev
```

---

## 📘 ШАГ 4: Проверка работы

### 1. Страница инструментов (`/tools`)
- Должна показывать дерево: Категория → Модель → Экземпляр
- Категории и модели разворачиваются по клику
- Экземпляры отображаются с инвентарным номером и статусом

### 2. Создание инструмента (`/tools/create`)
- Первый селект: выбор категории
- Второй селект: выбор модели (появляется после выбора категории)
- Поля: инвентарный номер, серийный номер (опционально)

### 3. Страница инструмента (`/tools/:id`)
- Показывает: категорию, модель, инвентарный номер, серийный номер, статус

---

## 🔧 Возможные проблемы и решения

### Проблема 1: "Cannot find module '../types/category.types'"
**Решение:** Убедитесь, что файлы созданы:
- `mishaGay/src/types/category.types.ts`
- `mishaGay/src/types/template.types.ts`

### Проблема 2: "404 Not Found" при запросах к API
**Решение:** Проверьте, что backend endpoints соответствуют путям в API-клиентах. Если endpoints под `/api/admin/`, обновите пути.

### Проблема 3: Типы не совпадают
**Решение:** Очистите кэш Vite (см. ШАГ 2) и перезапустите dev-сервер.

### Проблема 4: "ToolStatus" конфликты
**Решение:** Все компоненты теперь используют `ToolStatus` из `tool.types.ts`. Если видите ошибки, проверьте импорты.

---

## 📝 Структура данных

### ToolCategory
```typescript
{
  id: number;
  name: string;
  description?: string;
}
```

### ToolTemplate
```typescript
{
  id: number;
  name: string;
  description?: string;
  categoryId: number;
}
```

### Tool (экземпляр)
```typescript
{
  id: number;
  inventoryNumber: string;
  serialNumber?: string;
  status: ToolStatus; // "AVAILABLE" | "RENTED" | "OVERDUE" | "BROKEN" | "LOST" | "SERVICE"
  templateId: number;
  templateName: string;
  categoryId: number;
  categoryName: string;
  images: ToolImage[];
}
```

### CreateToolDto
```typescript
{
  templateId: number;
  inventoryNumber: string;
  serialNumber?: string;
}
```

---

## 🎯 Что дальше?

1. ✅ Проверьте работу всех страниц
2. ✅ Протестируйте создание инструментов
3. ✅ Проверьте отображение дерева на ToolsPage
4. ✅ Убедитесь, что все фильтры работают корректно

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте консоль браузера (F12)
2. Проверьте Network tab в DevTools
3. Убедитесь, что backend возвращает данные в правильном формате
4. Проверьте, что все импорты корректны

---

**Готово! 🎉** Frontend полностью интегрирован под новую архитектуру backend.

